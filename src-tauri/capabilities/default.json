{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Grants core APIs + event listen (updater banner), the updater plugin, and shell open. The app's OWN commands (open_external / install_update / apply_incremental_update) are allowed automatically once the window is covered by any capability — they don't take a permission entry.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "updater:default",
    "shell:allow-open",
    "allow-ipc"
  ]
}
